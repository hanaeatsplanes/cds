{
  "name": "@hanaeatsplanes/cds-cli",
  "version": "0.0.1",
  "author": "Sahana Celestine",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hanaeatsplanes/cds.git"
  },
  "module": "src/main.ts",
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "bin": {
    "cds-cli": "dist/cds-cli"
  },
  "bugs": {
    "url": "https://github.com/hanaeatsplanes/cds/issues"
  },
  "description": "This is the helper tool for https://cds.sahana.dev",
  "homepage": "https://github.com/hanaeatsplanes/cds#readme",
  "license": "WTFPL",
  "scripts": {
    "build": "bun run build:compile && bun run build:executable",
    "build:compile": "bun build . --production --outdir=dist",
    "build:executable": "bun build . --compile --outfile bin/cds-cli",
    "lint": "biome check .",
    "lint:fix": "biome check . --apply",
    "format": "biome format .",
    "format:fix": "biome format . --write",
    "check": "biome check .",
    "ci": "biome check . --error-on-warnings && bun run build"
  },
  "type": "module"
}
